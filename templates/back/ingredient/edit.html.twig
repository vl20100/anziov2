{% extends 'back/base_admin.html.twig' %}

{% block title %}Modifier {{ ingredient.name }} (#{{ ingredient.id }}) - Administranzio{% endblock %}

{% block container %}
    <h1>Modifier {{ ingredient.name }} (#{{ ingredient.id }}) - Administranzio</h1>

    {{ include('back/ingredient/_form.html.twig') }}

    <a href="{{ path('app_back_ingredient_index') }}" class="btn"><span class="fal fa-arrow-left"></span> Retour à la liste</a>

    {{ include('back/ingredient/_delete_form.html.twig') }}
{% endblock %}
