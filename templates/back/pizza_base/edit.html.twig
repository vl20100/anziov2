{% extends 'back/base_admin.html.twig' %}

{% block title %}Modifier {{ pizza_base.name }} (#{{ pizza_base.id }}) - Administranzio{% endblock %}

{% block container %}
    <h1>Modifier {{ pizza_base.name }} (#{{ pizza_base.id }})</h1>

    {{ include('back/pizza_base/_form.html.twig') }}

    <a href="{{ path('app_back_pizzabase_index') }}" class="btn"><span class="fal fa-arrow-left"></span> Retour à la liste</a>

    {{ include('back/pizza_base/_delete_form.html.twig') }}
{% endblock %}
