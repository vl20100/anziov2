<form method="post" class="deleteForm" action="{{ path('app_back_pizzabase_delete', {'id': pizza_base.id}) }}" onsubmit="return confirm('Supprimer la base de pizza {{ pizza_base.name }} (#{{ pizza_base.id }}) ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pizza_base.id) }}">
    <button class="btn deleteBtn"><span class="fal fa-trash-alt"></span> Supprimer</button>
</form>
